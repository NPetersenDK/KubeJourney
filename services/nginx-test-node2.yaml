apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-nd2
  labels:
    app: nginx-nd2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-nd2
  template:
    metadata:
      labels:
        app: nginx-nd2
    spec:
      nodeSelector:
        kubernetes.io/hostname: k3s-nd-02
      containers:
      - name: my-app-container
        image: nginx:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: my-service-nd2
  labels:
    bgp: "pool-a"
    bgp-announce: "true"
spec:
  selector:
    app: nginx-nd2
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
  externalTrafficPolicy: Local
